# 更新日志 v1.6

## 🚀 新增功能

### 1. 智能递归扫描与剪枝 (Smart Scanning)
*   **功能**: 实现了递归目录扫描器。
*   **剪枝优化**: 支持基于日期的“智能剪枝”。如果指定了 `start_date` 或 `end_date`，系统会直接跳过日期范围之外的文件夹，极大提升了扫描效率。
*   **解析逻辑**:
    *   **父级文件夹**: 强制采用 `yyyyMMdd-yyyyMMdd...` 或 `yyyyMMdd...` 的标准格式解析。
    *   **位置前缀**: 父文件夹的地点信息会自动作为前缀拼接到子文件夹地点中（例如：父 `2023_日本` + 子 `东京` -> 地点 `日本_东京`）。
    *   **混合解析**: `config` 中的自定义正则 (`structure_pattern`) 现在**仅作用于最后一层文件夹**，既保证了父级结构的规范性，又保留了末级文件夹命名的灵活性。

### 2. 备选结果与人工校对
*   **AI 备选**: 系统现在会将 Top 5（可通过 `top_k` 配置）识别结果存入数据库 (`candidates_json` 字段)。
*   **阈值控制**: 引入 `recognition.alternatives_threshold` (默认 70)。
    *   若 AI 置信度 > 70%，EXIF 备注仅写入首选结果。
    *   若 < 70%，EXIF 备注会列出所有备选项供参考。
*   **Web 交互**:
    *   “修正 ID”弹窗中新增了 **"AI 备选 (Alternatives)"** 列表，点击即可一键修正。
*   **智能重命名**:
    *   当人工修正物种名时，如果文件名模版包含 `{species_cn}`、`{species_sci}` 或 `{confidence}`，文件会被**自动重命名并移动**到正确路径。
    *   文件名中的置信度会自动更新为 `100pct` 以体现人工确认的状态。

### 3. NAS / WebDAV 支持
*   **支持**: 确认支持通过操作系统挂载的 WebDAV/SMB 网络驱动器。
*   **文档**: 新增 `docs/NAS_SETUP_zh.md`，提供详细的挂载指南。

## 🛠️ 改进与修复

### 元数据 (EXIF/IPTC/XMP)
*   **格式统一**: 将 "Title" 和 "Description" 字段统一格式化为 `中文名 (拉丁名)`。
*   **Subject 字段**: 显式清空 `XPSubject` 字段，避免在 Windows 资源管理器中显示重复或冗余信息。
*   **编码修复**: 通过将文件名放入参数文件 (`argfile`) 传递，彻底解决了 Windows CLI 下的中文路径编码问题。
*   **安全性**: 使用 `-E` 标志和 HTML 实体 (`&#xa;`) 安全处理备注中的换行符。

### 配置与逻辑
*   **正则优化**: 修正了 `structure_pattern` 的贪婪匹配问题，防止其错误吞噬父级目录路径。
*   **Top-K**: 确保本地模型和 API 模式均支持 `top_k` 参数配置。

### 数据库
*   **扫描历史**: 新增 `scan_history` 表，记录每次任务的耗时、处理数量和日期范围。
*   **自动迁移**: 启动时自动检测并添加 `candidates_json` 列，无需手动升级数据库。

## 👨‍💻 开发备注
*   **重构**: 将文件夹日期解析逻辑提取为 `PathParser.parse_folder_name` 静态方法，便于复用。
*   **API**: 升级 `/api/update_label` 接口，使其具备文件系统操作（移动/重命名）和 EXIF 回写能力。

---

## 🐛 修复 (v1.6 之后)

### Web 界面改进
*   **翻页一致性**: 修复了顶部和底部翻页按钮行为不一致的问题。现在两者都使用动态 JavaScript 渲染，状态同步正常。
*   **Limit 同步**: 修复 `currentLimit` 变量，从服务器同步 limit 值而非硬编码 50。
*   **布局修复**: 使用 CSS Grid 替换表格布局显示照片画廊，无论最后一行有多少张照片，都能保持4列等宽显示。
*   **分类树布局**: 调整侧边栏宽度为 `col-md-3` (25%)，与照片网格更平衡。

### 技术细节
```javascript
// 修改前：硬编码 limit
let currentLimit = 50;

// 修改后：从服务器同步 limit
let currentLimit = {{ limit }};

// CSS Grid 确保照片等宽
.photo-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
}
```
